<h1>Editing Book</h1>
<h4>Title</h4>
<%= form_for(@book) do |f| %>
    <%= f.text_field :title %>
    
    <h4>Body</h4>
    <%= f.text_area :body %>
    <br><%= f.submit 'update book' %>

    <br><%=link_to "Show", book_path(@book) %> |
    <%=link_to "Back", books_path %>

<% end %>
